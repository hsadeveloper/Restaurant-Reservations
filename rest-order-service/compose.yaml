services:
  postgresql:
    image: postgres:15
    container_name: rest-order
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: front
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
